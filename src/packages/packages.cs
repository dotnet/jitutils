using System;
using System.IO;

namespace ManagedCodeGen
{
    public class packages
    {
        public static void Main(string[] args)
        {
            Console.WriteLine("This project exsits only to pull in a particular set of " +
                              "dependencies for diffing.  See the corediff tool for the " +
                              "list of framework assembles used");
        }
    }
}
